# DKP Confluent Cloud Clients

Sample Kafka clients that authenticate to Confluent Cloud via Microsoft Entra ID (OAUTHBEARER / client-credentials) and run a produce-then-consume flow.

## Clients

- [`dotnet-client/`](dotnet-client/) — .NET console app (see `dotnet-client/EXECUTION.md`)
